const dbIp = 'mongodb://localhost:27017/MoonEd';
const serverPort = 3000; // best to keep the default
const socketPort = 8080; // webSocket port for direct messages, best to keep default

module.exports = {
    dbIp,
    serverPort,
    socketPort
}